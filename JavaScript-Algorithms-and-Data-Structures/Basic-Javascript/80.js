function isLess(a, b) {
  // Only change code below this line

  switch (a < b) {
    case true:
      return true;
      break;
    case false:
      return false;
      break;
  }
  // Only change code above this line
}

isLess(10, 15);